#!/usr/bin/env node
// bin/sleep-sort
var sleepsort = require('../lib/main');

var args = process.argv.slice(2);

function printArray(array) {
  console.log(array.join(' '));
}

sleepsort(args, printArray);

